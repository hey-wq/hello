package com.signidesign.dailytasks.ui.theme

import androidx.compose.ui.unit.dp

// Spacing scale — whitespace is the primary design element, so screens use
// these generous steps instead of ad hoc values.
object Dimens {
    val screenPadding = 24.dp
    val sectionGap = 28.dp
    val cardPadding = 20.dp
    val itemGap = 12.dp
    val innerGap = 8.dp
}
